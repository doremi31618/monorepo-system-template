# @platform/logger

**Framework：NestJS 10**

提供 `LoggerModule` 與環境感知的 `LoggerService`。開發環境使用 Nest console 格式，production 輸出 JSON。

```ts
import { LoggerModule, LoggerService } from '@platform/logger';
```
