NODE_ENV=dev
PORT=3333
API_BASE_URL=http://localhost:3333
HOST_URL=http://localhost:3333/v1
FRONTEND_URL=http://localhost:5173
WORKER_ID=local-api

DATABASE_URL=postgres://postgres:postgres@localhost:5432/postgres

# The API owns the bootstrap administrator. Packages only receive this as injected config.
ROOT_ADMIN_EMAIL=admin@example.com
ROOT_ADMIN_NAME=System Administrator
ROOT_ADMIN_PASSWORD=change-me-admin-password

# OAuth 2.1 / OpenID Connect. Production requires a private ES256 JWKS.
OAUTH_ISSUER=http://localhost:3333
MCP_PRIVATE_RESOURCE_URI=http://localhost:3333/mcp/private
OAUTH_PRIVATE_JWKS=
OAUTH_DCR_ENABLED=true
OAUTH_DCR_RESOURCES=http://localhost:3333/mcp/private
OAUTH_DCR_SCOPES=openid,email,profile,offline_access,mcp:tools
OAUTH_DCR_RATE_LIMIT=10
OAUTH_DCR_RATE_WINDOW_SECONDS=3600
OAUTH_TOKEN_RATE_LIMIT=120
AUTH_LOGIN_RATE_LIMIT=10

# Mail settings
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password
SMTP_FROM=no-reply@example.com

# Optional Google OAuth credentials.
GOOGLE_SSO_CLIENT_ID=your-google-client-id
GOOGLE_SSO_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:3333/v1/auth/google/callback

# Object storage (MinIO locally; S3-compatible providers in deployed environments).
STORAGE_PROVIDER=s3
STORAGE_REGION=us-east-1
STORAGE_ENDPOINT=http://localhost:9000
STORAGE_ACCESS_KEY=minioadmin
STORAGE_SECRET_KEY=minioadmin
STORAGE_BUCKET=r3-assets
STORAGE_FORCE_PATH_STYLE=true
